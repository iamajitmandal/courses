/*
    To connect BE & FE, we need cors package in the BE
        -> npm install cors

    CORS is an HTTP-header based mechanism that allows a server to indicate any origins (domain, scheme, or port) 
    other than its own from which a browser should permit loading resources. This is crucial for web applications
     where the frontend and backend might be hosted on different domains, preventing security restrictions imposed 
     by the browser's same-origin policy. 


*/