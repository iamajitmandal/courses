/*
    Project Disussion:
    
    For Projects, there must be:
        LOGIN
        Register
        Admin Panel

    Echarts for Dashboard
    D3.js

    Learning to start a basic Ecommerce ProductList page



    Try to understand the following two codes:
    
    1.
        {lotteryItems.map((item, id) => {
          return (
            <div
              key={id}
              style={{ backgroundColor: id === lotteryId ? "red" : null }}
              className="w-32 shadow-lg m-2 p-2"
            >
              {item}
            </div>
          );
        })}

    2.
        <div>
            {productLIst.map((item) => <ProductCard item = {item}/>)}
        </div>

    For rendering in one line, return keyword is not necessary


    


*/