/*
    Learning to start project

    Learning Project Structure:
        Learning codes of Main File sr/app/layout.js
        Learning codes of Next File src/app/page.js

        JSX -> HTML Tags 

        Learning to edit Pages.js and insert some HTML codes inside return statement

        Remove codes of Globals.css written inside src/app/globals/.css
        CSS Frameworks: plain css, tailwind, sass scss, bootstrap, emotion, styled components,
            Use Tailwind for react: Use Tailwind cheat sheet -> https://nerdcave.com/tailwind-cheat-sheet
            Styled Components: CSS Codes can be organized easily in styled components wheres in tailwind 
                                inline css is used, for styled components -> https://styled-components.com/

        Learning to use plain css in React:
            Give className in HTML tags in page.js file and add the css code in globals.css

        NextJs takes only one default export function if there are multiple functions in a page, default function
        will be loaded

        App Router Concept in NextJS:
            If you create a new folder inside app folder named register page and inside register page make page.js
            Now, go to the browser and run http://localhost:3000/register to run the page.js which is
            inside the register folder

        page.js inside src/app folder is the main home page of the website

        Finish todays issue today itself...

        Learning to create Login and Register Page...
        For that Make New Folder named Login and make page.js inside it and add login codes there
        For that Make New Folder named Register and make page.js inside it and add register codes there

        import React from 'react'

        const page = () => {
        return (
            // Login or Register Codes here            
        )
        }

        export default page
*/