/*
    Working on the facebook like button:
        Learning about Events: onMouseEnter, onMouseLeave, ..........


    Try to understand the following code using : if.....else or Ternary

        const handleColorChange = () => {
        if (color == 'grey'){
            setColor('skyblue')
        } else {
            setColor('grey')
        }
                or, 
        setColor(color === "grey" ? "blue" : "grey");

    In the facebook like button, try to solve the following issues:
        1. Default state should be like
        2. If you click on like, like button is grey color, change it into blue
        3. If we click on like from reaction div, and click it again, 
           button color should be grey

    Learning to make facebook Like Button:







*/