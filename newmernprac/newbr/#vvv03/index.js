/*
    Starting with the database connection:

    For MAC, homebrew install mongodb

    MERN, Does all company uses MongoDB 

    Most Popular Database in the differen ECOSYSTEM,
    
    For Express(Nodejs) -> Most Popular Database is Postgress 
                        -> Then Mongodb -> totally different than others -> non-relational database, noSQL, schemaless db
                        -> Then mysql
                        -> mssql (microsoft sql)
                        -> oracle

    For Laravel(php) -> mysql, 
                     -> oracle, mssql

    For Django(Python) -> Postgress
                       -> Mongoddb
    
    Java -> MSSQL, Oracle

    .net(c#) -> mssql, oracle

        SEO ->  NEXTJS SEO DOCUMENTATION LOG ROCKET DOCUMENTATION

    FOR PAYMENT GATEWAY,
        ESEWA PAYMENT API
        STRIPE API DOCUMENTATION

    NOSQL   ->  MongoDB, DynamoDB, CouchDB
    SQL     ->  Postgress, MSSQL, ORACLE, MYSQL

    SQL     ->  database, table (rows and columns)
    NOSQL   ->  database, collections (documents -> key value pairs : object based, json based)

    SQL -> ORM e.g. Sequelize
    NOSQL -> ODM e.g. Mongoose

    ORM SEQUELIZE

    ODM for Nodejs is Mongoose...so we see the documentation of Mongoose


*/