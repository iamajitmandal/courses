/* To be installed for MERN Series */

/*
    Set Environment Variables for Node:

    1. Install Git
        git -v                  -> to check git version and installed or not
        git config --list       -> Another way to check git installed or not in your machine

        For first time git installed and after running git config --list code 
        if username and email is not same as username and email in your github account then run the following code:

        git config --global user.email "theajitmandal@gmail.com"
*/

/*
*** For Frontend ***

        // before installing react, node must be installed
        1. npx create-next-app@latest           -> for react app

    For Client Side Form Validation:
        1. npm install formik --save                    1. Formik for Handling Form
        2. npm i yup                                    2. Yup for Form Validation 
                            (Alternatives Joe)

    Extras:
        1. React Icons
        2. BoxIcons
        3. React Slick for Carousels

    Starting: 
        
*/

/*
*** For Backend ***

    1. npm init -y                                      -> for package.json file
    2. npm install dotenv                               -> for environment variables (like port no, db connections)
    3. npm install express                              -> for express
    4. npm install nodemon                              -> for automatically starting server
    5. npm install router                               -> for router

    6. npm install mongoose

    7. npm install cors                                 -> for connecting BE & FE

    Starting:
        1. npm init -y
        2. npm install dotenv express nodemon router mongoose

*/

/*
*** Extension ***

    1. ES7+ React/Redux


*/
